import { Box } from '@mui/material';

const UsersPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default UsersPage;
