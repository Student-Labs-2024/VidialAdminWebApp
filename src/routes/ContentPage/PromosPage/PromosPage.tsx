import { Box } from '@mui/material';

const PromosPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default PromosPage;
