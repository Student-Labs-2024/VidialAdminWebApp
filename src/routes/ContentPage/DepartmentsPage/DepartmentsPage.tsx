import { Box } from '@mui/material';

const DepartmentPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default DepartmentPage;
