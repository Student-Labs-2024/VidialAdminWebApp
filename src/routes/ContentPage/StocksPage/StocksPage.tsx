import { Box } from '@mui/material';

const StocksPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default StocksPage;
