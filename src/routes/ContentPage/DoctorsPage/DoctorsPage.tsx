import { Box } from '@mui/material';

const DoctorsPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default DoctorsPage;
