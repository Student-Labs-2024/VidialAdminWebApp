import { Box } from '@mui/material';

const ServicesPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        width: '100%',
      }}
    ></Box>
  );
};

export default ServicesPage;
